{
  "scripts": {
    "build": "tsc --lib DOM,ESNext,DOM.Iterable,ScriptHost main.ts", 
    "start": "tsc -w --lib DOM,ESNext,DOM.Iterable,ScriptHost main.ts"
  },
  "devDependencies": {
    "@types/wicg-mediasession": "^1.0.3",
    "typescript": "^3.4.3"
  }
}
